__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
venv/

# Catálogo real: contém as redes e hosts internos de quem usa.
# O repositório carrega apenas data/profiles.example.toml.
data/profiles.toml

# Documentação de trabalho com dados de rede reais (auditorias, specs, planos).
docs/

# Lista de termos que não podem ser publicados — versioná-la entregaria a lista.
# O modelo genérico é .publicar-proibido.example.
.publicar-proibido
